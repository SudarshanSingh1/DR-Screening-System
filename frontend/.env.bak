# ─── Frontend Configuration ───────────────────────────────────────────────
# The base URL of the backend API.
# Note: Any VITE_ prefixed variable is exposed directly to the browser.
VITE_API_BASE_URL="/api/auth"
